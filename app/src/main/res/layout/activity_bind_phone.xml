<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_home_soft"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/bg_settings_nav"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp">

        <TextView
            android:id="@+id/btnBindBack"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_settings_row_clean"
            android:gravity="center"
            android:text="←"
            android:textColor="@color/brand_text_primary"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/bind_phone_title"
            android:textColor="@color/brand_text_primary"
            android:textSize="20sp"
            android:textStyle="bold" />

        <View
            android:layout_width="36dp"
            android:layout_height="36dp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginStart="12dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="12dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_settings_card_clean"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/bind_phone_hint"
                android:textColor="@color/brand_text_secondary"
                android:textSize="13sp" />

            <EditText
                android:id="@+id/etBindPhone"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_settings_row_clean"
                android:gravity="center_vertical"
                android:hint="@string/bind_phone_input_hint"
                android:inputType="phone"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textColor="@color/brand_text_primary"
                android:textColorHint="@color/brand_text_secondary" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="8dp"
                android:orientation="horizontal">

                <EditText
                    android:id="@+id/etBindCode"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@drawable/bg_settings_row_clean"
                    android:gravity="center_vertical"
                    android:hint="@string/bind_phone_code_hint"
                    android:inputType="number"
                    android:maxLength="6"
                    android:paddingStart="12dp"
                    android:paddingEnd="12dp"
                    android:textColor="@color/brand_text_primary"
                    android:textColorHint="@color/brand_text_secondary" />

                <Button
                    android:id="@+id/btnBindSendCode"
                    android:layout_width="130dp"
                    android:layout_height="match_parent"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_settings_row_clean"
                    android:text="@string/bind_phone_send_code"
                    android:textAllCaps="false"
                    android:textColor="@color/brand_text_primary" />
            </LinearLayout>

            <EditText
                android:id="@+id/etBindPassword"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_settings_row_clean"
                android:gravity="center_vertical"
                android:hint="@string/bind_phone_password_hint"
                android:inputType="textPassword"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textColor="@color/brand_text_primary"
                android:textColorHint="@color/brand_text_secondary" />

            <EditText
                android:id="@+id/etBindPasswordConfirm"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="8dp"
                android:background="@drawable/bg_settings_row_clean"
                android:gravity="center_vertical"
                android:hint="@string/bind_phone_password_confirm_hint"
                android:inputType="textPassword"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textColor="@color/brand_text_primary"
                android:textColorHint="@color/brand_text_secondary" />

            <TextView
                android:id="@+id/tvBindStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_settings_row_clean"
                android:padding="10dp"
                android:textColor="@color/brand_text_secondary"
                android:textSize="13sp"
                android:visibility="gone" />

            <Button
                android:id="@+id/btnBindSubmit"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/bg_settings_row_clean"
                android:text="@string/bind_phone_submit"
                android:textAllCaps="false"
                android:textColor="@color/brand_text_primary"
                android:textStyle="bold" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>
